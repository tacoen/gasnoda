{%- set invisible %}
    {% if segment.children %}
        {% for segment in segments %}
            {% include '/layout/' ~ segment.type ~ '.html.twig' with { 'segments': segment.children } %}
        {% endfor %}
    {% endif %}
{% endset -%}
